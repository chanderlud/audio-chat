output: dist/
releases:
  - name: distribution
    jobs:
      - name: release-distribution-linux-deb
        package:
          platform: linux
          target: deb

      - name: release-distribution-linux-rpm
        package:
          platform: linux
          target: rpm
