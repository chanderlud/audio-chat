output: dist/
releases:
  - name: distribution
    jobs:
      - name: release-distribution-linux-deb
        package:
          platform: linux
          target: deb
          architectures: [x64, aarch64]

      - name: release-distribution-linux-rpm
        package:
          platform: linux
          target: rpm
          architectures: [x64, aarch64]
